import "ws"
import "fmt"

fn onMessage(conn: ws.Conn, msg: string): void {
    fmt.println("got: " + msg)
    ws.send(conn, "echo: " + msg)
}

fn main(): void {
    ws.handleMessage(onMessage)
    fmt.println("WebSocket module compiled successfully")
}
