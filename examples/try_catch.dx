import "fmt"

fn main(): void {
    try {
        fmt.print("before")
        throw Exception("something broke")
        fmt.print("should not print")
    } catch (e: Exception) {
        fmt.print(e.message + " In catch")
    } finally {
        fmt.print("cleanup in finally")
    }
}
