import "http"
import "company"
import "user"
import "models/product"

fn main(): void {
  http.route("GET", "/companies", company.getAll())
  http.route("GET", "/users", user.list())
  http.route("GET", "/products", product.getAll())
  http.listen(8088)
}
