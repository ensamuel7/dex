import "fmt"
import "json"

fn main(): void {
    // Array literal + indexing
    let nums: int[] = [10, 20, 30]
    fmt.println(nums[0])

    // Index assignment + push
    nums[1] = 25
    nums.push(40)
    fmt.println(nums.len())

    // Stringify array to JSON
    let j: string = json.stringify(nums)
    fmt.println(j)

    // Embed array in JSON object
    let obj: string = json.new()
    obj = json.set(obj, "name", "test")
    obj = json.setArray(obj, "values", nums)
    fmt.println(obj)

    // Empty array
    let empty: int[] = []
    empty.push(1)
    fmt.println(empty[0])

    // Sort
    let sortable: int[] = [5, 2, 8, 1, 4]
    sortable.sort("asc")
    fmt.println(json.stringify(sortable))
    sortable.sort("desc")
    fmt.println(json.stringify(sortable))

    // Pop
    let last: int = sortable.pop()
    fmt.println(last)
    fmt.println(sortable.len())

    // Remove
    sortable.remove(0)
    fmt.println(json.stringify(sortable))

    // Contains
    let has5: bool = sortable.contains(5)
    fmt.println(has5)

    // IndexOf
    let idx: int = sortable.indexOf(4)
    fmt.println(idx)

    // Reverse
    let rev: int[] = [1, 2, 3]
    rev.reverse()
    fmt.println(json.stringify(rev))
}
