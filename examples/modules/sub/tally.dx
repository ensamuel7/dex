// Same top-level names as the counter module. Nothing here should be visible
// from there, or vice versa.

const LIMIT: int = 5
const LABEL: string = "tally"

let total: int = 1000

fn add(n: int): void {
    total = total + n
}

fn value(): int {
    return total
}
