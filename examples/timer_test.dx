import "time"
import "fmt"

fn heartbeat(): void {
    fmt.println("tick")
}

fn main(): void {
    let id: int = time.setInterval(heartbeat, 500)
    time.sleep(1200)
    time.clearInterval(id)
    fmt.println("done")
}
