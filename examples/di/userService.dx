import "fmt"

struct UserService(dbConnStr: string) {
  fn getUser(name: string): string {
    fmt.println("[UserService] querying " + dbConnStr + " for " + name)
    return "User(" + name + ")"
  }

  fn getUserName(): string {
    return "Kweku"
  }
}
