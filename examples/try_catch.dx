import "fmt"

fn main(): void {
    try {
        fmt.println("before")
        throw Exception("something broke")
        fmt.println("should not print")
    } catch (e: Exception) {
        fmt.println(e.message + " In catch")
    } finally {
        fmt.println("cleanup in finally")
    }
}
